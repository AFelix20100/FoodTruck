<form method="post" action="{{ path('app_food_truck_delete', {'id': food_truck.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ food_truck.id) }}">
    <button class="btn">Delete</button>
</form>
