{% extends 'base.html.twig' %}

{% block title %}FoodTruck index{% endblock %}

{% block body %}
    <!-- ======= Chefs Section ======= -->
    <section id="chefs" class="chefs section-bg mt-5">
      <div class="container" data-aos="fade-up">

        <div class="section-header">
          <h2>FoodTrucks</h2>
          <p>Découvrez les<span> FoodTrucks</span></p>
        </div>

        <div class="row gy-4">
        {% for food_truck in food_trucks %}
          <div class="col-lg-4 col-md-6 d-flex align-items-stretch" data-aos="fade-up" data-aos-delay="100">
              <div class="chef-member">
                <div class="member-img">
                        <img src="{{ asset('assets/img/chefs/chefs-1.jpg') }}" class="img-fluid" alt="">
                  <div class="social">
                    <a href=""><i class="bi bi-twitter"></i></a>
                    <a href=""><i class="bi bi-facebook"></i></a>
                    <a href=""><i class="bi bi-instagram"></i></a>
                    <a href=""><i class="bi bi-linkedin"></i></a>
                  </div>
                </div>
                    
                <div class="member-info">
                    <a class="h4" href="{{ path('app_food_truck_show', {'id': food_truck.id}) }}">{{ food_truck.nom }}</a>
                  <p>{{ food_truck.description }}</p>
                </div>
              </div>
          </div><!-- End Chefs Member -->
        {% endfor %}
        </div>
      </div>
    </section><!-- End Chefs Section -->
{% endblock %}
